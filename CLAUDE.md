# SEER prostate cancer — neural computation

This project uses neural computation (neuron) to investigate the SEER prostate
cancer dataset: grooming, modeling, evaluation, and notes that belong with the
cohort rather than with the neuron engine repo.

Large native data dumps (`.dta`, `.rds`) stay local and are not tracked in git.
