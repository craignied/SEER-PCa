# Large source dumps — too big for GitHub (and not needed in the remote)
*.dta
*.rds

.DS_Store
*.log

# Run artifacts under runs/ ARE tracked — they are the record of each modeling
# run. Re-admit them past the *.log rule above.
!runs/**/*.log
